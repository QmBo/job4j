package ru.job4j.game;
/*
import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.*;
*/
public class GameTest {
/*
    @Test
    public void whenCubeDropThenIntReturn() {
        Cube cube = new Cube();
        cube.drop();
        int[] result = cube.lastResult();
        assertTrue(result[0] > 0);
        assertTrue(result[0] < 7);
    }

    @Test
    void whenPlayerMoveThenNewPosition() {
        Player player = new Player();
        int oldPosition = player.getPosition();
        player.move(3);
        assertThat(player.getPosition(), is((oldPosition + 3)));
    }

    @Test
    public void whenGenerateFieldThenCellsList() {
        Field field = new Field();
        assertFalse(field.generate().isEmpty());
    }

    @Test
    public void whenPlayerOnCellThenListActions() {
        Cell cell = new Cell();
        assertFalse(cell.getActions().isEmpty());
    }
*/
}
